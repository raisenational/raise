# Sets default image - runs fast, and has everything we need
image: node:16.0-slim

pages:
  stage: deploy
  script:
    - mkdir .public
    - cp -r * .public
    - mv .public public
  artifacts:
    paths:
      - public
  only:
    - master

validate:
  stage: test
  script:
    - npx html-validate .