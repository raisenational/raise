# package directories
node_modules

# Serverless (and plugins) directories
.serverless
.webpack
.dynamodb

# jest output
coverage

# Custom environment
src/env/env.ts
src/env/local.ts
src/env/dev.ts
src/env/prod.ts
# local is .gitignored so custom changes aren't committed accidentally
# dev and prod configs are .gitignored to avoid leaking secrets
