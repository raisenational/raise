{
  "name": "@raise/shared",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "TZ=utc jest",
    "test:watch": "TZ=utc jest --watch",
    "lint": "eslint 'src'",
    "lint:fix": "npm run lint -- --fix"
  },
  "engines": {
    "node": ">=16"
  },
  "devDependencies": {
    "@raise/eslint-config": "*",
    "@raise/tsconfig": "*",
    "@types/jest": "^29.4.0",
    "@types/json-schema": "^7.0.9",
    "eslint": "^8.44.0",
    "jest": "^29.4.3",
    "ts-jest": "^29.0.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "collectCoverageFrom": [
      "src/**/*.ts"
    ]
  }
}
